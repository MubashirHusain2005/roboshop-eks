apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "robotshop-application.fullname" . }}-test-connection"
  labels:
    {{- include "robotshop-application.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "robotshop-application.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
