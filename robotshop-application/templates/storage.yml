apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: {{ .Values.mongo.storageClass }}
provisioner: {{ .Values.storage.provisioner }}
volumeBindingMode: {{ .Values.storage.volumeBindingMode }}
parameters:
  {{- toYaml .Values.storage.parameters | nindent 2 }}
---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: {{ .Values.mysql.storageClass }}
provisioner: {{ .Values.storage.provisioner }}
volumeBindingMode: {{ .Values.storage.volumeBindingMode }}
parameters:
  {{- toYaml .Values.storage.parameters | nindent 2 }}
---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: {{ .Values.redis.storageClass }}
provisioner: {{ .Values.storage.provisioner }}
volumeBindingMode: {{ .Values.storage.volumeBindingMode }}
parameters:
  {{- toYaml .Values.storage.parameters | nindent 2 }}

