apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}
  namespace: app-space
  labels:
    app: mysql-exporter
spec:
  selector:
    app: mysql-exporter
  ports:
    - name: metrics
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
